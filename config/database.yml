default: &default
  adapter: mysql2
  encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: root
  password:
  socket: <%= RUBY_PLATFORM =~ /darwin/ ? '/tmp/mysql.sock' : '/var/run/mysqld/mysqld.sock' %>

development:
  <<: *default
  database: pumpen_development

test:
  <<: *default
  database: pumpen_test

production:
  # Generated by Heroku
