<div class="col-md-12 col-s-12">
  <div class="card">
    <div class="card-block">
      <h3 class="card-subtitle mb-2 text-center"><%= t('bills.headline') %></h6>
        <% if @bills.any? %>
          <table class="table">
            <thead>
              <tr>
                <th><%= t('bills.month')%></th>
                <th><%= t('bills.number')%></th>
                <th><%= t('bills.sum')%></th>
                <th><%= t('bills.paid')%></th>
              </tr>
            </thead>
            <tbody>
              <%= render partial: @bills %>
            </tbody>
          </table>
        <% else %>
          <p><%= t('bills.empty') %></p>
        <% end %>
    </div>
  </div>
</div>
