<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8"/>
    <title>Crossfit HH Tabs Admin</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'admin', media: 'all' %>
    <%= javascript_include_tag 'admin' %>
  </head>
  <body>
    <%= render partial: 'shared/nav_bar_admin'%>
    <div class="container">
      <%= render partial: 'shared/notification'%>
      <div class="row">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
