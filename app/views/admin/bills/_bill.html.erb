<tr>
  <td><%= bill.tab.month %></td>
  <td><%= bill.number %></td>
  <td><%= bill.tab.user.first_name%> <%= bill.tab.user.last_name%></td>
  <td><%= link_to "Rechnung anzeigen", admin_bill_path(bill, format: :pdf)%></td>
  <td><%= bill.paid? %></td>
  <td><%= link_to "Rechnung bezahlt", admin_bill_path(bill.tab), method: :put %></td>
</tr>
