<div class="card">
  <div class="card-header">
    <h4 class="card-title text-center"><%= t('my_tab.list.headline')%></h4>
    <h6 class="card-subtitle mb-2 text-muted text-center">
      <%= t('my_tab.list.description', month: translated_month(@tab.month)) %>
    </h6>
  </div>
  <div class="card-block" id="tab-table">
    <%= render partial: 'tab_table' %>
  </div>
</div>
